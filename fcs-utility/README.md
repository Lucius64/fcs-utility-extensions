# fcs-utility

* Fixes an issue where an exception is thrown when attempting to display the context menu for a node reference.
* You can set the values for the dialog conditions and dialog effects up to a maximum of 10 million.
* Supports adding values to `EventTriggerEnum` and `DialogActionEnum`.
